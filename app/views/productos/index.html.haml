%h2 Productos

%table#productos
  %thead
    %tr
      %th Producto
      %th Precio
      %th 
      %th 
  %tbody
    - @productos.each do |producto|
      %tr
        %td= link_to "#{producto.nombre}",producto_path(producto) 
        %td= producto.precio_venta
        %td= link_to "Recetas con #{producto.nombre}", receta_path(producto.nombre)
        %td= link_to "Comprar #{producto.nombre}", productos_path(:comprado => "#{producto.nombre}")

-# hay que mejorarlo con javascrip (para el link del nombre del producto a la pagina de show) y el css